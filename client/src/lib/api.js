import axios from "axios";

const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL || "http://localhost:5000/api",
  withCredentials: true, // required so the browser sends/receives the httpOnly cookie automatically on every request
});

export default api;